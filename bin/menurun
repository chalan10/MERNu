#!/bin/bash
# menurun

# Stop on errors
set -Eeuo pipefail

if [ ! -f bin/menudb ]
then
	bin/menudb create
	echo "bin/menudb create"
fi

export FLASK_DEBUG=True
export MENU_SETTINGS=config.py

export FLASK_ENV=development
export FLASK_APP=menu
flask run --host 0.0.0.0 --port 8000
